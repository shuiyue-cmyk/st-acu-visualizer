# 数据库可视化前端

SillyTavern 的「SP·数据库」可视化前端扩展。在聊天区渲染数据库表格，支持表格浏览、单元格编辑、整体编辑、插入行、删除行、选项面板、仪表盘等功能。

> ⚠️ **停止维护公告**：本前端为同一份代码，目前同时适配 SillyTavern（ST 酒馆）与 TauriTavern（TT 酒馆）两个环境。自 **2026 年 9 月 7 日** 起将**停止对 ST 酒馆环境的适配维护**：此后所有更新仅在 TT 酒馆验证，ST 侧兼容问题（含数据库版本更新后出现的）不再处理，ST 上的可用性不再保证。如有需要，请提前迁移至 TT 酒馆。（ST 用户更新到 V17.4.10 后会看到一次弹窗提示）

## 安装（无需手动导入 JSON）

1. 打开 SillyTavern
2. 进入 `Extensions`
3. 使用 **「Install extension from Git URL」** 入口
4. 粘贴本仓库地址：
   ```
   https://github.com/shuiyue-cmyk/st-acu-visualizer
   ```
5. 安装完成后刷新 SillyTavern

安装后扩展列表中会出现 `数据库可视化前端`。无需手动导入任何 JSON 文件。

## 环境要求

- SillyTavern `1.12.13+`（已在 `1.18.0` 上测试）
- 已安装并启用「SP·数据库」插件（本前端依赖其 `AutoCardUpdaterAPI` / `AutoCardUpdaterV2API` 公共接口）

## 功能

- 聊天区底部渲染数据库全部表格（全局数据表、主角信息表、重要角色表、主角技能表、背包物品表、任务与事件表、纪要表、选项表等）
- 表格分页浏览、搜索、倒序、多选删除
- 单元格就地编辑 / 整行编辑 / 插入新行
- 选项表 → 「行动选项」面板（点击自动填入输入框）
- 仪表盘自定义（槽位绑定表格）
- 底部按钮：打开数据库工作台（V2 新 UI）、打开表格编辑器、一键追平所选表未填楼层、全能设置、收起/展开
- 主题：数据库 UI 风格下拉（关闭/苹果/香薰衣草）+ 前端 15 主题；点击兜底保证追平/填表后数据即时可见
- 运行时数据提示：数据库内存态更新（未落盘聊天）时前端 toast 提示，避免误当已保存

## 与数据库版本适配

| 前端版本 | 适配数据库 | 说明 |
|---|---|---|
| V17.5.2 | 9.2.4 + rebuild 9.1.1（双库） | 首个双库兼容戳：上游 AlbusKen/shujuku 9.2.4 与自家 TT 库 shujuku-rebuild 9.1.1（792a3c0）公共契约双探针全绿——13 调用点双在位签名一致、精确写布尔返回、persisted 同式归一、export 活引用、豁免集同集，前端无逻辑变更 |
| V17.5.1 | 9.2.4（插件版） | 上游 9.1→9.2.4（33 笔：API 预设协议四选一 PR#10/API 后处理 PR#11/模板冲突 fail-closed/SQL isolatedRuntime 可选分支重构/续写与填表修复；CRUD API 层仅空白变更、SQL 默认路径语义不变、API_DOCUMENTATION.md 整文件删除纯文档），公共契约探针全兼容，前端无逻辑变更 |
| V17.4.10 | 9.1（插件版） | 停维公告文案订正：明确本前端为同一份代码双宿主适配、停的是 ST 环境适配维护（9-7 起更新仅在 TT 验证）；记标键升 v2 使已见旧文案的用户重收一次订正版 |
| V17.4.9 | 9.1（插件版） | ST 宿主首次加载弹一次停维公告（2026-09-07 起停止 ST 环境适配维护） |
| V17.4.8 | 9.1（插件版） | 上游 9.1（续写缓存加固/seed/交火/SQL 手动填表修复，均在 continuation/worldbook/agent 域），公共契约探针全兼容，前端无逻辑变更 |
| V17.4.7 | 9.0（插件版） | 移除调试采集模块；数据库内存态更新（未落盘聊天）时 toast 提示 |
| V17.4.6 | 9.0（插件版） | 修复与 9.0 储存结构冲突：精确写失败不再降级全量导入、移除随机 sheetKey 合成、克隆防活引用外泄、刷新后清多选/待删态等六项 |
| V17.4.5 | 9.0（插件版） | 原生位置恢复 `#chat` 末尾直挂 + 缺失重建自愈，修复进角色卡前端消失（V17.4.3/4.4 为该链路中间态，已被取代） |
| V17.4.2 | 9.0（插件版） | 数据库改为标准扩展插件（AlbusKen/shujuku），公共契约探针全兼容，前端无逻辑变更 |
| V17.3.6 | 8.9.2 | 适配 8.9.2 填表提示词强制更新（`spv8.9.2-force-default-table-fill-prompt`，向后兼容，无需前端逻辑变更） |
| V17.3.5 | 8.9.2 | 点击兜底保证追平即时可见 |
| V17.3.4 | 8.9.2 | 稳固性修复（跨聊天误删/脏缓存/渲染守卫等） |
| V17.3.3 | 8.9.2 | 修复表格点击不展开回归（H-02 回退） |
| V16.4 | 8.9 | - |
| V16.3 | 8.8 | - |
| V16.2 | 8.7.2 | - |
| V15.x | 7.5 ~ 8.1.x | - |

## 维护

- 原作者：夏休期
- 二改：a1185673398
- 三改：星火（已休眠）
- 现维护：Crino

---

## 支持我

我一直在用 **OpenCode Go** 的套餐游玩酒馆，如果你也喜欢用 AI 游玩酒馆，欢迎使用我的专属链接一起获得额外 **5 美元** 额度：

**https://opencode.ai/go?ref=S2XBM2KR89**

> 通过此链接注册/使用 OpenCode Go，你将直接支持本项目的持续维护 ❤️ — 感谢每一位支持者！
